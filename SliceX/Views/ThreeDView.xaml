<UserControl x:Class="SliceX.Views.ThreeDView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hx="http://helix-toolkit.org/wpf"
             xmlns:vm="clr-namespace:SliceX.ViewModels"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converters="clr-namespace:SliceX.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent"
             AllowDrop="True">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Drag Overlay Converter -->
        <converters:BooleanToDragOverlayConverter x:Key="BooleanToDragOverlayConverter"/>
        
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3e3e42"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Drag Drop Overlay -->
        <Border x:Name="DragDropOverlay"
                Background="{Binding IsDragOver, Converter={StaticResource BooleanToDragOverlayConverter}}"
                Visibility="{Binding IsDragOver, Converter={StaticResource BooleanToVisibilityConverter}}"
                CornerRadius="10"
                Margin="20"
                BorderBrush="#007ACC"
                BorderThickness="2"
                Panel.ZIndex="1000">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="20" Color="#007ACC" Opacity="0.7"/>
            </Border.Effect>
            <Viewbox Stretch="Uniform">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="📁" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding DragDropMessage}" 
                               FontSize="16" 
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"/>
                    <TextBlock Text="Release to load model" 
                               FontSize="12"
                               Foreground="#CCCCCC"
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Viewbox>
        </Border>

        <!-- 3D Viewport -->
        <hx:HelixViewport3D x:Name="viewport"
                            Background="#1e1e1e"
                            ZoomExtentsWhenLoaded="True"
                            ShowViewCube="True"
                            IsRotationEnabled="{Binding IsModelLoaded}"
                            IsPanEnabled="{Binding IsModelLoaded}"
                            IsZoomEnabled="{Binding IsModelLoaded}"
                            AllowDrop="True">

            <!-- Default lighting -->
            <hx:DefaultLights/>

        </hx:HelixViewport3D>

        <!-- Status and Controls Overlay -->
        <Border Background="#80000000" CornerRadius="5" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="10">
            <StackPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontWeight="Bold"/>
                <Button Content="🗑️ Unload Model" 
                        Command="{Binding UnloadModelCommand}"
                        Style="{StaticResource ModernButton}"
                        Margin="0,5,0,0"
                        Padding="10,5"
                        Visibility="{Binding IsModelLoaded, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>