<UserControl x:Class="SliceX.Views.ThreeDView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:h="http://helix-toolkit.org/wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1e1e1e">

    <Grid>
        <h:HelixViewport3D x:Name="Viewport3DControl"
                           ZoomExtentsWhenLoaded="True"
                           ShowViewCube="True"
                           ShowCameraInfo="False"
                           ViewCubeOpacity="0.9"
                           ViewCubeWidth="80"
                           ViewCubeHeight="80"
                           IsRotationEnabled="True"
                           IsPanEnabled="True"
                           IsZoomEnabled="True"
                           IsInertiaEnabled="True"
                           CameraMode="Inspect"
                           CameraRotationMode="Trackball"
                           InfiniteSpin="False"
                           Background="#1e1e1e">

            <h:DefaultLights/>
            <h:GridLinesVisual3D Width="200" Length="200" MinorDistance="10" 
                                MajorDistance="50" Thickness="0.5" Fill="#2d2d30"/>
        </h:HelixViewport3D>

        <!-- Status Overlay -->
        <Border Background="#CC000000" CornerRadius="5" Padding="10" 
                HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10">
            <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>