<Window x:Class="SliceX.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:SliceX.Views"
        xmlns:vm="clr-namespace:SliceX.ViewModels"
        Title="SliceX - 3D Printing Software"
        Height="900"
        Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e1e"
        Icon="/Resources/CMTILogo.ico">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3e3e42"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Navigation Button Style -->
        <Style x:Key="NavButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3f3f46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
    </Window.Resources>

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Navigation Bar -->
        <Border Grid.Row="0" Background="#252526" BorderBrush="#3f3f46" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="🖥️ 3D View" Command="{Binding NavigateTo3DViewCommand}" 
                        Style="{StaticResource NavButton}" Margin="5,0"/>
                <Button Content="💻 GCode" Command="{Binding NavigateToGCodeCommand}" 
                        Style="{StaticResource NavButton}" Margin="5,0"/>
                <Button Content="👁️ Slice Viewer" Command="{Binding NavigateToSliceViewerCommand}" 
                        Style="{StaticResource NavButton}" Margin="5,0"/>
                <Button Content="⚙️ Machine Control" Command="{Binding NavigateToMachineControlCommand}" 
                        Style="{StaticResource NavButton}" Margin="5,0"/>
                <Button Content="🔧 Machine Config" Command="{Binding NavigateToMachineConfigCommand}" 
                        Style="{StaticResource NavButton}" Margin="5,0"/>
                <Button Content="⚡ Slice Profile" Command="{Binding NavigateToSliceProfileCommand}" 
                        Style="{StaticResource NavButton}" Margin="5,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar - Fixed -->
            <views:SidebarView Grid.Column="0" DataContext="{Binding}"/>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="5" Background="#3f3f46"/>

            <!-- Right Content Area -->
            <ContentControl Grid.Column="2" Content="{Binding CurrentView}"/>
        </Grid>
    </Grid>
</Window>